import type { ControlPosition, DraggableData, DraggableOptions, MouseTouchEvent } from './types';
export declare function getBoundPosition({ bounds, x, y, node, }: {
    bounds: any;
    x: number;
    y: number;
    node: HTMLElement | SVGElement;
}): [number, number];
export declare function snapToGrid(grid: [number, number], pendingX: number, pendingY: number): [number, number];
export declare function canDragX(axis: DraggableOptions['axis']): boolean;
export declare function canDragY(axis: DraggableOptions['axis']): boolean;
export declare function getControlPosition({ e, touch, node, offsetContainer, scale, }: {
    e: MouseTouchEvent;
    touch: number | undefined;
    node: HTMLElement | SVGElement;
    offsetContainer?: HTMLElement;
    scale: number;
}): ControlPosition | null;
export declare function createCoreData({ node, x, y, lastX, lastY, }: {
    node: HTMLElement | SVGElement;
    x: number;
    y: number;
    lastX: number;
    lastY: number;
}): DraggableData;
export declare function createDraggableData({ x, y, data }: {
    x: number;
    y: number;
    data: DraggableData;
}): DraggableData;
